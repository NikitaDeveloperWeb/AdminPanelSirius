import React from 'react';
import styled from '@emotion/styled';

let GraphSection = styled.section({});
let DateIntervalPanel = styled.div({});
function Graph() {
  return (
    <GraphSection>
      <DateIntervalPanel></DateIntervalPanel>
    </GraphSection>
  );
}

export default Graph;
